using System;
using System.Linq.Expressions;

namespace Medical.AppLayer.Managers
{
    public class SearchManager
    {
       /*readonly ISearchService _service;

       public SearchManager(ISearchService service)
        {
            _service = service;
        }


       public Expression<Func<T, bool>> GetPatient<T>() where T : class
        {
            return _service.GetPatient<T>();
        }

        public Expression<Func<T, bool>> GetEvent<T>() where T : class
        {
            return _service.GetEvents<T>();
        }*/

        
    }
}
